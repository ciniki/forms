#
# Description
# -----------
# Store the field definitions for forms.
#
# Fields
# ------
# id:                       The ID assigned to the record.
# uuid:                     The Universal Unique ID.
# tnid:                     The Tenant ID the record belongs to.
#
# section_id:               The ID of the section the field is attached to.
#
# ftype:                    The type of field.
#
#                               text (Text 1 Line)
#                               date
#                               number
#                               phone
#                               email
#                               address
#                               url
#                               select
#                               radio
#                               checkbox (single checkbox, multiple joined together when no label)
#                               textarea (Text multiline)
#                               content (label and/or description, no fields)
#                               image   (stored in ciniki_images, image_id in form_data)
#                               document (stored in ciniki-storage dir, as uuid from form_data)
#                               newline (setup a new line within a section in layout)
#                               break (setup a new field group within a section)
#
# flags:                    The options for the field.
#
#                               0x01 - Required field
#                               0x02 - Hide responses from Jurers
#                               0x04 - Submission Label (use this field as the title for this submission).
#                               0x08 - Locked (no edits by customer allowed, pulled from default values).
#                               0x10 - Juror Label (must also be visible to jurors !0x02)
#                               0x20 - 
#                               0x40 - 
#                               0x80 - 
#
# sequence:                 The order for the field in the form.
#
# prefill_ref:              The reference to another module/object where the field data can pulled from.
#
# field_ref:                The reference to another field where the field data can be pushed to.
#                           format: package.module.object.field,  eg: ciniki.customers.customer.first
#
# field_size:               The size of the field, used to scale fields in forms online and print pdf.
#
#                               tiny - future
#                               small - 20 rem
#                               small-medium - 20-40 rem
#                               medium - 40 rem
#                               medium - 40 rem
#                               large - 80 rem
#
# label:                    The label for the field/content. 
# description:              The optional description for the field.
#
# options:                  JSON encoded options for the field. Used to store select/radio options, checkbox label, etc.
#
# date_added:               The UTC date and time the record was added.
# last_updated:             The UTC date and time the record was last update.
#
create table ciniki_form_fields (
    id int not null auto_increment,
    uuid char(36) not null,
    tnid int not null,

    section_id int not null,
    ftype varchar(50) not null,
    flags smallint unsigned not null,
    sequence smallint unsigned not null,
    prefill_ref varchar(250) not null,
    field_ref varchar(250) not null,
    field_size varchar(50) not null,
    label varchar(250) not null,
    description text not null,
    options text not null,

    date_added datetime not null,
    last_updated datetime not null,
    primary key (id),
    unique index (uuid),
    index sync (tnid, uuid, last_updated)
) ENGINE='InnoDB', COMMENT='v1.03';
